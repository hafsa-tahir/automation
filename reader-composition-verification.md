# Reader Composition Verification

The desktop reader was verified with `/books/510001/preview?spread=0`. It displays one story scene as a fixed open book: the complete illustration-only leaf is on the left, the matching text-only leaf is on the right, and the center spine is thin. No generated copy is clipped.

The mobile reader was verified at 390×844 with the same spread query. It displays the readable text leaf for the selected scene within a contained book frame.
